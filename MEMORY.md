2026-09-22: Chặn ngày trả dự kiến sớm hơn ngày bắt đầu thuê (gây số ngày thuê/doanh thu âm) ở cả 3 lớp: input date có `min`/`max` ràng buộc lẫn nhau + validate trước submit ở NewOrderModal.tsx; validate trong `taoDonThue()`; và CHECK constraint `don_thue_ngay_hop_le` ở DB (migration 0006, thêm dạng `NOT VALID` vì đã có đơn cũ — đơn đã huỷ #C51531A6 — mang dữ liệu sai, không muốn chặn migration chạy). Chỉ có duy nhất `taoDonThue()` tạo `ngay_bat_dau`/`ngay_tra_du_kien` — chưa có tính năng sửa ngày sau khi tạo đơn, nên không cần sửa chỗ nào khác. Đơn cũ bị sai (đã huỷ) vẫn còn nguyên trong DB, không tự sửa được — muốn dọn thì sửa tay qua SQL Editor.
2026-09-22: Fix lỗi React #441 khi tạo đơn mới chọn thiết bị đang bị đơn khác giữ — cùng nguyên nhân/cách sửa với vụ xoá thiết bị trước đó: `taoDonThue()` giờ `redirect()` ngay trong Server Action thay vì client `router.push()+router.refresh()` (né Next tự render lại trang cũ), trả về `{error?}` thay vì `throw` cho lỗi nghiệp vụ, client bỏ try/catch quanh lời gọi. **Ghi nhớ chung**: đây là lỗi thứ 2 cùng dạng gặp phải — bất kỳ Server Action nào điều hướng sang trang khác sau khi thành công đều nên dùng `redirect()` server-side, KHÔNG dùng `router.push()+router.refresh()` ở client, để tránh lỗi #441 y hệt lặp lại ở các action khác trong tương lai (vd nếu sau này thêm action "chuyển chặng rồi điều hướng" tương tự). Cách debug hiệu quả: dựng trang test tạm mount thẳng component thật + Playwright headless để tái hiện đúng luồng UI, đọc `.next/dev/logs/next-development.log` của dev server thật (không cần chạy server riêng) để xem lỗi đầy đủ không bị rút gọn.
2026-09-22: Thêm bộ lọc khoảng thời gian ở màn Báo cáo (Tháng này mặc định / Tháng trước / Lựa chọn khoảng thời gian tuỳ ý). Lọc theo query param `?from=&to=` (giống pattern lọc chặng ở /orders), tiêu chí lọc là **ngày bắt đầu thuê** nằm trong khoảng — không phải ngày kết thúc hay ngày tạo đơn. `layBaoCaoThietBi()` nay nhận thêm 2 tham số tuỳ chọn tuNgay/denNgay.
2026-09-22: Fix nút Xuất báo giá PDF không phản hồi — 2 lỗi cộng dồn: (1) đoạn "Lời cảm ơn" dùng `fontStyle: "italic"` nhưng chỉ đăng ký font normal/bold, react-pdf không resolve được nên toBlob() reject; (2) code không có try/catch nên lỗi bị nuốt im lặng, nút chỉ lặng lẽ tắt "Đang xuất...". Đã thêm try/catch + hiện badge lỗi trong XuatBaoGiaButton.tsx. Nhân tiện phát hiện thêm: matcher của proxy.ts quên loại trừ .ttf/.woff/.css/.js tĩnh nên các file này (vd font PDF) bị proxy chặn/redirect /login khi chưa đăng nhập — đã mở rộng danh sách phần mở rộng loại trừ trong matcher. Cách debug: dựng route test tạm (`test-pdf-tmp`, phải KHÔNG bắt đầu bằng `_` vì Next coi đó là private folder) + Playwright headless để bắt đúng lỗi console thật, xoá sạch sau khi xong.
2026-09-22: Xuất báo giá PDF ở trang chi tiết đơn (nút chỉ hiện từ chặng Báo giá trở lên, ẩn ở Yêu cầu/Huỷ). Dùng `@react-pdf/renderer` (render client-side, không cần server route riêng), font Be Vietnam Pro tự tải về `public/fonts/` (OFL, hỗ trợ đầy đủ tiếng Việt — font mặc định của react-pdf không có dấu). Thông tin công ty (CINEB PRODUCTION, MST 0402190079, 22 Giang Văn Minh Hoà Cường Đà Nẵng, liên hệ Bảo/Linh) + VAT 8% + toàn bộ điều khoản giờ làm nhân sự/thanh toán/lưu ý đã hard-code trong `lib/pdf/BaoGiaDocument.tsx` theo đúng nội dung user cung cấp — sau này đổi giá VAT hay điều khoản thì sửa trực tiếp file đó. Logo: hoá ra đã có sẵn ở `app/public/cineb-logo.png` từ lúc khởi tạo project (ngày 1/9) — không cần upload lại, `DON_VI.logoUrl` đang trỏ đúng file này.
2026-09-22: Đồng bộ Google Calendar cá nhân — đã bàn với user và chọn phương án feed .ics (subscribe URL) thay vì đẩy trực tiếp qua Google Calendar API, vì đơn giản hơn nhiều và không cần xin thêm quyền OAuth cho từng thành viên (đánh đổi: đồng bộ có độ trễ vài giờ theo chu kỳ Google tự quyết, một chiều app→Google). Route `/calendar.ics` (route.ts, ngoài route group (app) vì Google fetch không gửi cookie) bảo vệ bằng `?key=` so với env var `CALENDAR_FEED_SECRET` (không dùng session) — đã thêm `/calendar.ics` vào danh sách bỏ qua kiểm tra đăng nhập trong proxy.ts. Link subscribe hiện ở cuối trang /calendar (SyncGoogleCalendarBox.tsx), người dùng tự copy rồi add vào Google Calendar qua "Other calendars → From URL". CẦN thêm `CALENDAR_FEED_SECRET` vào Environment Variables trên Vercel (đã có sẵn trong .env.local local).
2026-09-22: Fix logic Tổng quan — trước đây "Đơn thuê đang diễn ra"/"Yêu cầu sắp tới" chỉ lọc theo chặng (chang), không đối chiếu ngày thuê thực tế, nên đơn quá hạn/tương lai bị xếp sai chỗ. Đã thống nhất với user: "Đang diễn ra" = đã giao/chờ trả VÀ hôm nay nằm trong dải ngày; "Sắp tới" = yêu cầu/báo giá VÀ ngày bắt đầu >= hôm nay (báo giá không còn tính là "đang diễn ra"); thêm khối mới "⚠️ Đơn quá hạn trả" = đã giao/chờ trả nhưng đã qua ngày trả dự kiến, để không mất dấu đơn cần đòi thiết bị. Xem `queries/tongQuan.ts`.
2026-09-22: Thêm dropdown lọc Danh mục ở màn Thiết bị (4 nhóm cố định: Ống kính/Lens, Body máy quay, Đèn & Ánh sáng, Grip & Phụ kiện + Toàn bộ). Do cột `danh_muc` trong DB vẫn là text tự do (nhập tay lúc thêm/import), lọc theo cách so khớp từ khoá không dấu (vd chọn "Body máy quay" thì khớp mọi dòng có chữ "body" trong `danh_muc`) thay vì so khớp tuyệt đối, để không bỏ sót dữ liệu cũ gõ khác cách viết. Component `EquipmentFilters.tsx` lọc phía client (kèm luôn ô tìm kiếm tên/mã vốn trước đó chỉ là input trang trí, chưa có chức năng thật).
2026-09-21: Thêm sửa/xoá/đổi trạng thái thiết bị (trên trang chi tiết `/equipment/[id]`, chỉ vai Chủ thấy — dùng `useVai()`). Thêm 2 trạng thái mới "hong" (Hỏng) và "thanh_ly" (Đã thanh lý) vào `trang_thai` (migration 0005, phải chạy tay trong Supabase SQL Editor). Sửa giá thuê sẽ ghi thêm dòng mới vào `lich_su_gia` (giữ lịch sử giá cũ). Xoá thiết bị bị chặn ở DB nếu đã có lịch sử cho thuê/kiểm tra (lỗi FK 23503) — action bắt lỗi này và gợi ý đánh dấu Hỏng/Thanh lý thay vì xoá.
2026-09-21: Fix cảm giác "nút bấm bị trễ" — nguyên nhân chính là mọi trang trong (app) đều `force-dynamic` nhưng không có `loading.tsx` nào, nên bấm Link chuyển trang màn hình đứng im vài trăm ms không phản hồi gì. Thêm `(app)/loading.tsx` (skeleton) làm Suspense fallback dùng chung cho mọi trang con. Đồng thời các nút hành động trong OrderActions.tsx đổi chữ thành "Đang lưu.../Đang xử lý..." khi pending thay vì chỉ mờ nút.
2026-09-20: Thêm đăng nhập Google qua Supabase Auth (bảng `members` chứa email/ten/vai, quản trị viên thêm sẵn — không có trang tự đăng ký). Chưa đăng nhập bị proxy.ts (Next 16 đổi tên từ middleware.ts) đẩy về /login; không có trong `members` thì bị đăng xuất ngay kèm thông báo "Bạn không có quyền truy cập". Thêm màn hình "Thành viên" (/members, chỉ Chủ) để Chủ tự thêm/sửa/xoá tài khoản trong `members` — không tự xoá/tự hạ quyền chính mình được. Deploy Vercel: Root Directory phải là `app` (không phải gốc repo) và Framework Preset phải là "Next.js" (không phải "Other"/"Node.js"), vì repo gốc còn có docs/ nên Vercel không tự nhận diện đúng.
2026-09-20: Thêm màn hình thứ 5 "Báo cáo" (/reports, chỉ hiện với vai Chủ — chuOnly). Gồm KPI tổng doanh thu/lợi nhuận/số đơn hoàn tất, bảng xếp hạng lợi nhuận & tần suất theo thiết bị, và danh sách thiết bị chưa từng được thuê. Quy ước tính toán: doanh thu/lợi nhuận chỉ tính đơn ở chặng "xong"; lượt thuê & tổng ngày thuê tính trên mọi đơn chưa huỷ (kể cả đang chạy).
2026-09-01: Chốt 4 màn hình CineB (Tổng quan, Đơn thuê, Thiết bị, Calendar) kèm logic cọc giấy tờ (CCCD/Bằng lái), hiển thị ngày thuê & số ngày, import Excel thiết bị, và thanh tìm kiếm/lọc danh mục thiết bị trong modal tạo đơn.
